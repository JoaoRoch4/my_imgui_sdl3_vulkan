// doctest test runner entry point for the parallel image module.
// Not linked into the application — built as the standalone `image_tests` target.
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest.h>
